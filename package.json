{
  "name": "pandjico4",
  "version": "1.0.0",
  "description": "Portfolio site",
  "scripts": {
    "build": "node build.js",
    "serve": "python3 -m http.server 8000",
    "dev": "npm run build && npm run serve"
  }
}

